import React from 'react';
import menu from '../menu.svg';

export const Menu = () => {
    return (
        <div className="menu-icon">
            <img src={menu} alt="menu" />
        </div>
    );
};
